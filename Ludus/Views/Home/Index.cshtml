@model Ludus.Models.Calendar
@{
    ViewBag.Title = "Home Page";
}
@if (Request.IsAuthenticated)
{
using (Html.BeginForm()) {
    if (Session["IndexView"] == "Month")
    {
    <input type="submit" class="btn btn-warning" value="Switch to List View" />@*
@*<div class="container-fluid">
    <div class="row-fluid">
        <div class="span10">*@
            @* include the Calendar view (for now) *@
            @Html.Partial("../Home/_CalendarPartial")
@*        </div>
    </div>
</div>*@
    }
    if (Session["IndexView"] == "List")
    {
    <input type="submit" class="btn btn-warning" value="Switch to Month View" />
@* include the List view (for now) *@
@Html.Partial("../Home/_ListPartial")
    }
    }
}
@if (!Request.IsAuthenticated)
{
<div>Login as Student1/student1 to operate as a student<br />
Login as Faculty1/faculty1 to operate as a student<br />
Login as Admin1/admin1 to operate as a student</div>
}